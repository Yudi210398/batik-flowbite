import { InputProps } from "./Input";

export default function Select(props: InputProps) {
  const {
    label,
    name,
    classnew = "",
    pencarian,
    toucheds,
    error,
    option,
    ...rest
  } = props;
}
